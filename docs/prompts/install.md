You will work on generating a new codebase that showcases all the blueprints by stacks for webapp and server.
Blueprints I have for now are in @blueprints.
Generate a new pnpm workspace with /webapp for now that lists all the blueprints.
The webapp should be Vite + React.
