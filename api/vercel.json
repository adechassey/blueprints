{
	"buildCommand": "pnpm --filter @blueprints/shared build && rm -rf node_modules/@blueprints/shared && cp -r ../packages/shared node_modules/@blueprints/shared && drizzle-kit migrate",
	"builds": [
		{
			"src": "index.ts",
			"use": "@vercel/node",
			"config": {
				"excludeFiles": "{node_modules/.pnpm/onnxruntime-node*/**/*cuda*,node_modules/.pnpm/onnxruntime-node*/**/*tensorrt*,node_modules/onnxruntime-node/**/*cuda*,node_modules/onnxruntime-node/**/*tensorrt*}"
			}
		}
	],
	"routes": [{ "src": "/(.*)", "dest": "index.ts" }]
}
